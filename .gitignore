# Environment variables and local config (CONTIENT LES CREDENTIALS)
.env.local
.env.production
# js/config.js was removed: the project is hosted on GitHub Pages and uses
# js/config.public.js (committed) so the site can boot in production.

# IDE
.idea/
.vscode/
*.swp
*.swo
*.sublime-workspace

# OS
.DS_Store
Thumbs.db

# Firebase (ne pas commiter la db locale)
.firebase/

# Aider (AI coding tool)
.aider*
